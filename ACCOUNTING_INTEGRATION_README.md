# 💼 Интеграция с бухгалтерской системой

## 🎯 Возможности

Теперь ваш Club Assistant Bot поддерживает:

1. **📸 Отправка чеков** - сканируйте QR-коды с чеков ФНС и отправляйте в бухгалтерию
2. **📦 Отправка накладных** - сохраняйте информацию о накладных с фото
3. **📋 История операций** - просмотр отправленных чеков и накладных

---

## 🚀 Как использовать

### 1. Отправить чек

1. Откройте главное меню (команда `/start`)
2. Нажмите **💼 Бухгалтерия**
3. Выберите **📸 Отправить чек**
4. Отсканируйте QR-код с чека и отправьте текст
5. Выберите категорию расхода (необязательно)
6. Подтвердите отправку

**Пример QR-кода с чека**:
```
t=20240115T1530&s=1500.00&fn=9999078900004792&i=12345&fp=3522207165&n=1
```

### 2. Отправить накладную

1. Откройте главное меню (команда `/start`)
2. Нажмите **💼 Бухгалтерия**
3. Выберите **📦 Отправить накладную**
4. Введите название поставщика
5. Введите сумму
6. Отправьте фото накладной (необязательно, можно пропустить командой `/skip`)
7. Введите описание (необязательно, можно пропустить командой `/skip`)
8. Подтвердите отправку

### 3. Просмотр истории

В меню **💼 Бухгалтерия** доступны:
- **📋 История чеков** - последние 10 отправленных чеков
- **📄 История накладных** - последние 10 накладных

---

## 🔐 Доступ

Функции бухгалтерии доступны только **владельцу бота** (owner_id из конфигурации).

---

## 📊 Данные

Все чеки и накладные сохраняются в локальной базе данных `knowledge.db` в таблицах:
- `sent_receipts` - отправленные чеки
- `invoices` - накладные

---

## 🔧 Технические детали

### API бухгалтерской системы

**URL**: `http://64.188.83.12:8000`

**Endpoints**:
- `POST /api/receipt` - прием чека
- `POST /api/cash-withdrawal` - выдача наличных под отчет
- `POST /api/accountable-report` - отчет по подотчету

### Модули

- `/opt/club_assistant/modules/accounting_receipts.py` - основной модуль
- `/opt/club_assistant/ACCOUNTING_BOT_INSTRUCTIONS.md` - инструкции для AI на стороне бухгалтерской системы

---

## ❓ Часто задаваемые вопросы

### Где взять QR-код с чека?

QR-код находится внизу любого чека от ККТ в России. Он содержит информацию о покупке и начинается с `t=`.

### Можно ли отправлять старые чеки?

Да, QR-коды действительны неограниченное время. Система проверяет их через API ФНС.

### Что делать если чек не найден?

Возможные причины:
- Неправильно отсканирован QR-код
- Чек еще не появился в базе ФНС (может занять до 24 часов)
- Продавец не передал данные в ФНС

Попробуйте позже или свяжитесь с бухгалтером для ручной обработки.

### Можно ли редактировать отправленные чеки?

Нет, после отправки чек остается неизменным. Если нужно исправить - обратитесь к администратору системы.

---

## 🆘 Поддержка

При проблемах проверьте:
1. Доступность API бухгалтерской системы (`http://64.188.83.12:8000`)
2. Логи бота: `journalctl -u club_assistant -f`
3. Таблицы БД: `sqlite3 knowledge.db "SELECT * FROM sent_receipts LIMIT 5;"`

---

## 📝 История изменений

**v1.0** (02.11.2025)
- Добавлена отправка чеков по QR-коду
- Добавлена отправка накладных с фото
- Добавлен просмотр истории операций
- Создана интеграция с бухгалтерским API

---

**Готово к использованию! 🎉**
