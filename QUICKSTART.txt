# ⚡ БЫСТРЫЙ СТАРТ - Club Assistant Bot

## 🎯 Установка за 3 команды

```bash
# 1. Распакуйте архив
unzip club_bot_final.zip -d /opt/club_assistant
cd /opt/club_assistant

# 2. Запустите установщик
chmod +x install.sh
sudo ./install.sh

# 3. Готово!
```

Скрипт спросит токены и всё настроит сам!

---

## 📝 Что вам понадобится

Перед запуском получите:

1. **Telegram Bot Token**
   - Telegram → [@BotFather](https://t.me/botfather)
   - `/newbot` → следуйте инструкциям
   - Скопируйте токен вида: `1234567890:ABCdef...`

2. **OpenAI API Key**
   - [platform.openai.com/api-keys](https://platform.openai.com/api-keys)
   - Create new secret key
   - Скопируйте ключ вида: `sk-proj-...`

3. **Ваш Telegram ID**
   - Telegram → [@userinfobot](https://t.me/userinfobot)
   - `/start`
   - Скопируйте число (например: `123456789`)

---

## 🚀 Выбор установки

Установщик предложит 2 варианта:

### 1️⃣ Обычная установка (systemd)
- ✅ Проще
- ✅ Меньше зависимостей
- ✅ Быстрее
- **Рекомендуется для начинающих**

### 2️⃣ Docker установка
- ✅ Изолированная среда
- ✅ Легко переносить
- ✅ Автообновление контейнера

---

## 📋 Команды управления

### Systemd:
```bash
systemctl status club_assistant   # Статус
systemctl restart club_assistant  # Перезапуск
journalctl -u club_assistant -f   # Логи
```

### Docker:
```bash
docker-compose ps              # Статус
docker-compose restart         # Перезапуск
docker-compose logs -f         # Логи
```

---

## 🎮 Команды бота

### Для всех:
- Любой вопрос → ответ

### Для админа:
- `/learn текст` - умное обучение
- `/stats` - статистика
- `/forget слово` - удалить
- `/update` - обновить с GitHub

---

## 🔄 Автообновление с GitHub

### 1. Создайте репозиторий
GitHub → New repository → `club-bot` (Private)

### 2. Загрузите код
```bash
cd /opt/club_assistant
git init
git add bot.py requirements.txt .gitignore
git commit -m "Initial"
git remote add origin https://github.com/USER/club-bot.git
git push -u origin main
```

### 3. Добавьте в config.json
```json
{
  ...
  "github_repo": "https://github.com/USER/club-bot.git"
}
```

### 4. Используйте
В Telegram: `/update`

---

## 💡 Пример обучения

```
Админ: /learn Клуб на улице Ленина 123, второй этаж

Бот: ✅ Запомнил!
     ❓ Где находится клуб?
     💬 ул. Ленина, 123, 2 этаж
     📁 location

Пользователь: где вы?
Бот: 💡 ул. Ленина, 123, 2 этаж
```

---

## 🆘 Проблемы?

### Бот не запускается
```bash
# Смотрим логи
journalctl -u club_assistant -n 50
# или
docker-compose logs --tail=50
```

### Бот не отвечает
```bash
# Проверяем config
cat /opt/club_assistant/config.json
```

### Нет команды
```bash
# Проверяем что вы админ
# admin_ids в config.json должен содержать ваш ID
```

---

## ✅ Проверка работы

1. Найдите бота в Telegram (по username из BotFather)
2. Напишите `/start`
3. Бот должен ответить приветствием
4. Если админ - увидите команды `/learn`, `/update`

---

## 🎉 Готово!

Теперь:
- ✅ Бот работает 24/7
- ✅ Отвечает на вопросы
- ✅ Учится сам
- ✅ Обновляется одной командой

**Наслаждайтесь!** 🚀
